---
phase: 04-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/styles/globals.css
  - frontend/components/ui/skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Shimmer animation displays smooth gradient across skeleton elements"
    - "All skeleton loaders have proper ARIA attributes for screen readers"
    - "Shimmer animation is calmer and more professional than basic pulse"
  artifacts:
    - path: "frontend/styles/globals.css"
      provides: "Shimmer animation keyframes"
      contains: "@keyframes shimmer"
    - path: "frontend/components/ui/skeleton.tsx"
      provides: "Enhanced Skeleton component with ARIA"
      exports: ["Skeleton"]
  key_links:
    - from: "frontend/components/ui/skeleton.tsx"
      to: "frontend/styles/globals.css"
      via: "animate-shimmer class"
      pattern: "animate-shimmer"
    - from: "Skeleton component"
      to: "screen readers"
      via: "ARIA attributes"
      pattern: 'role="status"'
---

<objective>
Add shimmer animation and accessibility to base Skeleton component

Purpose: Provide professional loading experience with smooth gradient animation and proper screen reader support
Output: Enhanced Skeleton component with shimmer variant and full ARIA compliance
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-loading-states/04-RESEARCH.md

@frontend/components/ui/skeleton.tsx
@frontend/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shimmer animation keyframes to global CSS</name>
  <files>frontend/styles/globals.css</files>
  <action>
    Add shimmer animation keyframes to frontend/styles/globals.css:

    1. After line 2 (@import statements), add shimmer keyframes:
       ```css
       @keyframes shimmer {
         0% { background-position: -1000px 0; }
         100% { background-position: 1000px 0; }
       }

       .animate-shimmer {
         animation: shimmer 2s infinite linear;
         background: linear-gradient(
           to right,
           hsl(var(--muted)) 0%,
           hsl(var(--muted) / 0.5) 50%,
           hsl(var(--muted)) 100%
         );
         background-size: 1000px 100%;
       }
       ```

    Why 2s duration: Research recommends 1.5-2s for calm, professional feel without being distracting

    Do NOT modify any existing CSS - only add the new keyframes and class
  </action>
  <verify>
    Shimmer class defined in globals.css:
    grep -q "shimmer" frontend/styles/globals.css && echo "Shimmer found"
  </verify>
  <done>
    Shimmer animation keyframes and CSS class added to globals.css
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Skeleton component with ARIA attributes</name>
  <files>frontend/components/ui/skeleton.tsx</files>
  <action>
    Update frontend/components/ui/skeleton.tsx to add accessibility:

    1. Add ARIA attributes to the div:
       - role="status"
       - aria-live="polite"
       - aria-label="Loading content"

    2. Update className to use shimmer variant (replacing animate-pulse):
       Change: "bg-accent animate-pulse rounded-md"
       To: "bg-accent animate-shimmer rounded-md"

    Do NOT change any other props or behavior - only accessibility and animation

    Reference: Pattern 4 from RESEARCH.md (Accessible Loading with ARIA Attributes)
  </action>
  <verify>
    ARIA attributes present in component:
    grep -E 'role=|aria-live=|aria-label=' frontend/components/ui/skeleton.tsx && echo "ARIA attributes found"
  </verify>
  <done>
    Skeleton component has shimmer animation and full ARIA accessibility support
  </done>
</task>

</tasks>

<verification>
1. Shimmer animation displays smooth gradient animation (not generic pulse)
2. All skeleton elements announce "Loading content" to screen readers
3. No TypeScript errors after changes
4. Existing skeleton components still render correctly
</verification>

<success_criteria>
1. Shimmer keyframes defined in globals.css with 2s animation cycle
2. Skeleton component uses animate-shimmer class instead of animate-pulse
3. Skeleton component has role="status", aria-live="polite", aria-label attributes
4. Zero console errors or TypeScript errors
5. All existing skeleton components automatically inherit shimmer and accessibility
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-states/04-01-SUMMARY.md`
</output>
