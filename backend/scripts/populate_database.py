#Data Population Script: In backend/scripts, you might include a script, populate_database.py,
# to load initial embeddings and similarity data directly into your MongoDB cluster. 
# Run this script locally or in a cloud environment, so you can avoid repeated, real-time calculation calls.