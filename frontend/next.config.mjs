/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.myanimelist.net'], // Allows Next.js to load images from MyAnimeList
    },
  };
  
  export default nextConfig;
  